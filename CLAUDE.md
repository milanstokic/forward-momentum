# forward-momentum

## What this is

A new project. The stack is not yet decided — start with `/spec` to define scope before writing code.

## How we work here

This repo uses the `agent-skills` plugin (enabled in `.claude/settings.json`). Follow the
spec → plan → build → test → review → ship lifecycle. Don't skip the spec for anything
non-trivial; let the relevant skills drive the process.

## Conventions

- Branch off `main` for new work; keep commits small and atomic.
- Tests are proof — a feature isn't done until it's verified.
- Update this file as the project's stack and conventions get decided.
